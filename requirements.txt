# Versions pinned to what is verified-installed in the project venv.
# Unpinned entries are resolved on first install and pinned per stage.

# Telegram bot
aiogram==3.13.1

# Database
SQLAlchemy[asyncio]==2.0.49
alembic==1.18.4
aiosqlite==0.20.0
greenlet==3.1.1

# Scraping (Stage 1+)
playwright==1.48.0
playwright-stealth==2.0.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Models / config
pydantic==2.9.2
python-dotenv==1.0.1

# Image download (Stage 2: dhash dedup, with Avito CDN headers)
httpx==0.27.2

# AI / image (Stage 2+, local only)
torch==2.11.0
torchvision==0.26.0
transformers==5.8.0
pillow==10.4.0
numpy==2.4.4
